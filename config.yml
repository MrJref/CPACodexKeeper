# CPACodexKeeper configuration template.
# Uncomment values here to override environment variables, including those from docker-compose.yml.
#
# cpa:
#   endpoint: https://your-cpa-endpoint
#   token: your-management-token
#   cron: "0 0/10 * * * ?"
#   # Cron random offset window: interval_min runs early, interval_max runs late.
#   interval_min: 10m
#   interval_max: 30m
#   quota_threshold: 1
#   expiry_threshold_days: 3
#   enable_refresh: true
#   enable_auto_delete: true
#   http_timeout: 30
#   usage_timeout: 15
#   max_retries: 2
#   worker_threads: 8
#
# openai:
#   proxy:
#
# webui:
#   enabled: true
#   host: 0.0.0.0
#   port: 8765
#   log_max_lines: 500
#
# auth:
#   enabled: false
#   login_password: replace-with-your-login-password
#   session_ttl: 168h
